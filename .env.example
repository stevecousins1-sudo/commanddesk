PORT=3000
DB_HOST=postgres
DB_PORT=5432
DB_NAME=commanddesk
DB_USER=commanddesk
DB_PASSWORD=changeme
NODE_ENV=production

# Optional: comma-separated list of origins allowed to call the API cross-origin.
# Leave unset — the React app is served same-origin, so no cross-origin access is needed.
# CORS_ORIGIN=https://yourapp.example.com
